---
title: "Vowel duration"
author: "Stefano Coretta"
date: \today
output: 
  pdf_document: 
    fig_caption: yes
    latex_engine: xelatex
    number_sections: yes
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
knitr::opts_knit$set(root.dir=normalizePath("../../"))
library(readr)
library(dplyr)
library(purrr)
library(lme4)
library(afex)
library(effects)
library(ggplot2)
theme_set(theme_bw())
```

# Import data

Let's read the files.

```{r read-files}
vowels <- list.files(path = "pilot/results",
                   pattern = "*-vowel-durations.csv",
                   full.names = TRUE) %>%
    map_df(~read_csv(.))

languages <- read_csv("./pilot/stimuli/languages.csv")
words <- read_csv("./pilot/stimuli/nonce.csv")


vowels <- left_join(vowels, languages) %>%
    left_join(y = words) %>%
    mutate_if(is.character, as.factor) %>%
    mutate(duration.norm = (duration - mean(duration)) / sd(duration))
```

# Italian

Fit a mixed effect linear model for Italian.

```{r lmer-it}
vowels.it <- filter(vowels, language == "italian")

model.it <- lmer(duration.norm ~ c2phonation * vowel + c1phonation + c2place +
                  (1 | word) + (1|speaker),
              data = vowels.it
              )

summary(model.it)

mixed(duration.norm ~ c2phonation * vowel + c1phonation + c2place +
                  (1 | word) + (1|speaker),
              data = vowels.it)
```

```{r italian-effects-plot}
plot(allEffects(model.it))
```

# Polish

Fit a mixed effect linear model for Polish

```{r}
vowels.pl <- filter(vowels, language == "polish")

model.pl <- lmer(duration ~ c2phonation * vowel + c2place +
                  (1 | word),
              data = vowels.pl
              )

summary(model.pl)

mixed(duration ~ c2phonation * vowel + c2place +
                  (1 | word),
              data = vowels.pl)
```

```{r polish-effects-plot}
plot(allEffects(model.pl))
```

# Inter-speaker variation

```{r variation}
dodge <- position_dodge(width = 0.5)

ggplot(filter(vowels.it, c2place == "velar"), aes(c2phonation, duration, colour = speaker)) +
    facet_grid(. ~ vowel) +
    geom_violin(position = dodge) +
    geom_boxplot(width=0.1, position = dodge)

ggplot(filter(vowels.it, c2place == "coronal"), aes(c2phonation, duration, colour = speaker)) +
    facet_grid(. ~ vowel) +
    geom_violin(position = dodge) +
    geom_boxplot(width=0.1, position = dodge)

ggplot(vowels.pl, aes(c2phonation, duration)) +
    facet_grid(. ~ vowel + c2place) +
    geom_violin() +
    geom_boxplot(width=0.2)
```
